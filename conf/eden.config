process {
    executor = 'slurm'
    queue    = 'short'       // adjust to EDEN’s naming
    cpus     = 30
    memory   = '50 GB'
    time     = '12h'

    // Example: load modules at job start (you’ll adjust these)
    beforeScript = '''
        module load bwa
        module load samtools
        module load fastqc
        module load python
    '''
}

workDir = '/scratch/$USER/isage_work'  // or whatever EDEN recommends
