// conf/eden_local.config

process {
    executor = 'local'
    cpus     = 20        // how many CPUs you want Nextflow to use on the node
    memory   = '100 GB'  // per-process ceiling (just a guideline)
    time     = '24h'
}

workDir = "/home2/faculty/pmishra/I-SAGE_work"
