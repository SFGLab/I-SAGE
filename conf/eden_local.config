// conf/eden_local.config

process {
    executor = 'local'
    cpus     = 6        // how many CPUs you want Nextflow to use on the node
    memory   = '60 GB'  // per-process ceiling (just a guideline)
    time     = '12h'
}

workDir = "/home2/faculty/pmishra/I-SAGE_work"
